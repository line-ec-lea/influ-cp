{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "check": "next typegen && tsc --noEmit && biome check --write . && eslint . --max-warnings 0 --fix",
    "check:ci": "next typegen && tsc --noEmit && biome check . && eslint . --max-warnings 0",
    "start": "next start",
    "test": "vitest",
    "test:ci": "vitest run --coverage --coverage.provider v8 --coverage.reporter text --coverage.reporter json-summary --coverage.reporter json --coverage.reporter html --coverage.reportsDirectory ./coverage --coverage.all true --coverage.include 'app/**/*.{ts,tsx}' --coverage.include 'env.ts' --coverage.exclude '**/*.{test,spec}.{ts,tsx}' --coverage.exclude '**/*.d.ts'"
  },
  "dependencies": {
    "@repo/ui": "workspace:*",
    "@t3-oss/env-nextjs": "^0.13.11",
    "next": "16.2.4",
    "react": "19.2.5",
    "react-dom": "19.2.5",
    "valibot": "^1.3.1"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.2",
    "@types/node": "^25.6.0",
    "@types/react": "19.2.14",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "^5.2.0",
    "@vitest/coverage-v8": "^4.1.5",
    "babel-plugin-react-compiler": "^1.0.0",
    "eslint-config-next": "16.2.4",
    "jsdom": "^29.1.1",
    "tailwindcss": "^4.2.4",
    "typescript": "6.0.3",
    "vitest": "^4.1.5"
  }
}
